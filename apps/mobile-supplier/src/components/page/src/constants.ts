export interface PageProps {
  topBarHeight?: number;
  tabBarHeight?: number;
  isTopBar?: boolean;
  isTabBar?: boolean;
  isSide?: boolean;
}

export const DEFAULT_TOP_BAR_HEIGHT = 40;
export const DEFAULT_TAB_BAR_HEIGHT = 55;
